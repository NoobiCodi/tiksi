/**
 * @class Ping 
 * A test class for debugging
 */
class Ping {
    /**
     * ping function
     * @param {int} i 
     */
    ping(obj) {
        return obj.i;
    }
}

module.exports = {
    Ping
};